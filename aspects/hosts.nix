{
  den.hosts.x86_64-linux = {
    ash = {
      users.ferret = { };
    };

    ember = {
      desktop.compositor = "niri";
      desktop.terminal = "foot";

      users.ferret = { };
    };

    interloper = { };
  };
}
